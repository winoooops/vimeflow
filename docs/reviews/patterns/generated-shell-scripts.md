---
id: generated-shell-scripts
category: backend
created: 2026-06-02
last_updated: 2026-06-02
ref_count: 0
---

# Generated Shell Scripts

## Summary

Shell scripts generated by the Rust backend (e.g., PTY session shims, init
scripts) must be defensively written because they run in unpredictable user
environments. Generated bash must guard against unset env vars, handle `exec`
failures gracefully, and remove competing shell functions/aliases before
relying on PATH resolution. A small oversight in a generated one-liner becomes
a cryptic runtime failure for every user session.

## Findings

### 1. Shim passes `--settings ""` when VIMEFLOW_CLAUDE_SETTINGS is unset

- **Source:** github-claude | PR #325 round 1 | 2026-06-02
- **Severity:** MEDIUM
- **File:** `crates/backend/src/terminal/bridge.rs`
- **Finding:** The generated claude shim executed `exec "$candidate" --settings "$VIMEFLOW_CLAUDE_SETTINGS" "$@"` with no guard on the env var. When invoked outside a Vimeflow session (e.g., stale PATH entry after cleanup), Claude received `--settings ""` and failed with an opaque file-not-found error.
- **Fix:** Added an early guard: `if [ -z "${VIMEFLOW_CLAUDE_SETTINGS:-}" ]; then ... exit 1; fi` that prints a clear diagnostic before the PATH loop.
- **Commit:** same commit as this entry

### 2. `set -e` exits shim on exec failure, skips remaining PATH entries

- **Source:** github-claude | PR #325 round 1 | 2026-06-02
- **Severity:** MEDIUM
- **File:** `crates/backend/src/terminal/bridge.rs`
- **Finding:** The shim used `set -e` globally. Inside the PATH search loop, `exec "$candidate"` could fail after passing the `-x` guard (TOCTOU race, ENOEXEC, EACCES). In non-interactive bash, a failed `exec` terminates the shell before any `|| continue` fallback is evaluated, so later valid `claude` binaries in PATH were never tried.
- **Fix:** Removed global `set -e`, added explicit `|| { ...; exit 1; }` on the `cd` line only, enabled `shopt -s execfail` before the loop, and appended `|| continue` to the `exec` line so exec failures fall through to the next PATH entry.
- **Commit:** same commit as this entry

### 3. Clear claude functions before relying on PATH shim

- **Source:** github-codex-connector | PR #325 round 1 | 2026-06-02
- **Severity:** P2 / MEDIUM
- **File:** `crates/backend/src/terminal/bridge.rs`
- **Finding:** The shell init script only ran `unalias claude 2>/dev/null` before prepending the shim directory to PATH. A pre-existing `claude()` shell function in the user's startup files still took precedence over the PATH shim, bypassing `--settings` injection and breaking the statusline bridge.
- **Fix:** Added `unset -f claude 2>/dev/null` immediately after `unalias claude 2>/dev/null` in the generated shell init script.
- **Commit:** same commit as this entry
