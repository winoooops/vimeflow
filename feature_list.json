[
  {
    "id": 1,
    "phase": 1,
    "category": "dependencies",
    "description": "Add @floating-ui/react dependency",
    "steps": [
      "Run `npm install --save @floating-ui/react` (or `unproxy && npm install ...` if proxy error)",
      "Verify install with `node -e \"console.log(require('@floating-ui/react/package.json').version)\"`",
      "Verify package.json has @floating-ui/react entry",
      "Commit package.json + package-lock.json"
    ],
    "passes": true,
    "dependencies": []
  },
  {
    "id": 2,
    "phase": 1,
    "category": "scaffold",
    "description": "Tooltip primitive scaffold with disabled/null short-circuit",
    "steps": [
      "Create src/components/Tooltip.test.tsx with disabled and null-content tests",
      "Run tests to verify they fail (module not found)",
      "Create src/components/Tooltip.tsx with TooltipProps interface and short-circuit logic",
      "Run tests to verify they pass",
      "Commit both files"
    ],
    "passes": true,
    "dependencies": [1]
  },
  {
    "id": 3,
    "phase": 1,
    "category": "implementation",
    "description": "Wire Floating UI hooks (interactions + a11y)",
    "steps": [
      "Add 5 new tests to Tooltip.test.tsx: hover open, mouse-leave close, focus open, Escape close, accessible description",
      "Add userEvent import",
      "Run tests to verify they fail (tooltip never renders)",
      "Replace Tooltip.tsx scaffold with real implementation: useFloating + useHover (safePolygon) + useFocus + useDismiss + useRole + useInteractions + useMergeRefs + FloatingPortal",
      "Run tests to verify all 7 tests pass",
      "Commit both files"
    ],
    "passes": true,
    "dependencies": [2]
  },
  {
    "id": 4,
    "phase": 1,
    "category": "testing",
    "description": "Configuration props tests (placement, maxWidth, ref preservation, className)",
    "steps": [
      "Add 4 new tests to Tooltip.test.tsx: placement data-attribute, maxWidth style, ref preservation, className append",
      "Add createRef import from React",
      "Run tests to verify all 11 tests pass (implementation already plumbs these props from feature 3)",
      "Commit test file"
    ],
    "passes": false,
    "dependencies": [3]
  },
  {
    "id": 5,
    "phase": 1,
    "category": "integration",
    "description": "Migrate IconRail to the new Tooltip primitive",
    "steps": [
      "Overwrite IconRail.tsx: add Tooltip import, remove useState hover state, remove relative wrappers, remove z-50 patch, wrap buttons in <Tooltip>",
      "Replace IconRail.test.tsx tooltip tests with 2 integration-only assertions (content flows correctly)",
      "Add userEvent import if missing",
      "Run IconRail.test.tsx to verify all tests pass",
      "Commit both files"
    ],
    "passes": false,
    "dependencies": [4]
  },
  {
    "id": 6,
    "phase": 1,
    "category": "integration",
    "description": "Reveal full event body via Tooltip in ActivityEvent",
    "steps": [
      "Add Tooltip import to ActivityEvent.tsx",
      "Replace body <div> at line 156 with <Tooltip><span tabIndex={0}></span></Tooltip> (focusable span with focus-visible ring)",
      "Add 2 tests to ActivityEvent.test.tsx: tooltip on hover shows full body, body span has tabIndex 0",
      "Add userEvent import if missing",
      "Update existing tests if they assert div selector (change to span)",
      "Run ActivityEvent.test.tsx to verify all tests pass",
      "Commit both files"
    ],
    "passes": false,
    "dependencies": [5]
  },
  {
    "id": 7,
    "phase": 1,
    "category": "verification",
    "description": "Final verification (full test suite, type-check, lint, smoke-test)",
    "steps": [
      "Run `npm run test` (all tests pass)",
      "Run `npm run type-check` (clean exit)",
      "Run `npm run lint` (clean exit)",
      "Run `unproxy && npm run tauri:dev` and manually verify: rail tooltips portal over sidebar, Tab navigation works, activity tooltips reveal full body, focus rings visible, no console warnings",
      "Confirm z-50 patch removed: `grep -n 'z-50' src/features/workspace/components/IconRail.tsx || echo 'patch removed'`",
      "No commit needed unless follow-up fixes required"
    ],
    "passes": false,
    "dependencies": [6]
  }
]
