# Copy to scripts/linear.env (gitignored) and `source` it — the `export` makes
# the key visible to the linear-status.sh child process, not just your shell.
# Do NOT commit a real key.
# Create at: Linear → Settings → Security & access → Personal API keys
export LINEAR_API_KEY=lin_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
