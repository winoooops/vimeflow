# Copy to scripts/linear.env (gitignored) and `source` it, or export directly.
# Do NOT commit a real key.
# Create at: Linear → Settings → Security & access → Personal API keys
LINEAR_API_KEY=lin_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
